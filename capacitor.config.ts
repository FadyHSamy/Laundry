import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fadyhany.laundry',
  appName: 'Laundry',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
